server:
  port: 8282

spring:
  application:
    name: datasources

mysql:
  datasource:
    # DB1
    db1:
      url: jdbc:mysql://localhost:53306/XUZ_DEMO?useUnicode=true&characterEncoding=utf-8
      username: root
      password: 123456
      minPoolSize: 3
      maxPoolSize: 25
      maxLifetime: 20000
      borrowConnectionTimeout: 30
      loginTimeout: 30
      maintenanceInterval: 60
      maxIdleTime: 60
      testQuery: select 1
    # DB2
    db2:
      url: jdbc:mysql://localhost:53306/XUZ_DEMO2?useUnicode=true&characterEncoding=utf-8
      username: root
      password: 123456
      minPoolSize: 3
      maxPoolSize: 25
      maxLifetime: 20000
      borrowConnectionTimeout: 30
      loginTimeout: 30
      maintenanceInterval: 60
      maxIdleTime: 60
      testQuery: select 1

# mybatis配置
#mybatis:
#  type-aliases-package: com.xuz.db2.pojo
#  mapper-locations: classpath:db2-mapper/*.xml
#  # 表字段映射
#  configuration:
#    map-underscore-to-camel-case: true

